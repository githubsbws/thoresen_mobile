# ThorConn — Expo SDK 54

## วิธี Setup (ทำตามลำดับนี้เลย)

### 1. copy ไฟล์เข้า thorconn_final
แตก zip แล้ว copy ไฟล์/folder ทั้งหมดทับใน thorconn_final
(ยกเว้น node_modules และ .expo)

### 2. ติดตั้ง dependencies
```bash
cd C:\thorconn_final
npm install --legacy-peer-deps
```

### 3. รัน
```bash
npx expo start --clear
```

### 4. สแกน QR บน iPhone ได้เลย
